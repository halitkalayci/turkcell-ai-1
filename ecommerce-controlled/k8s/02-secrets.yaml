apiVersion: v1
kind: Secret
metadata:
  name: ecom-secrets
  namespace: ecom
  labels:
    app: common
    managed-by: kubectl
type: Opaque
stringData:
  # Database passwords (HUMAN: Replace with real values)
  DB_PASSWORD: "changeme"
  
  # Keycloak credentials (HUMAN: Replace with real values from Keycloak setup)
  KEYCLOAK_ADMIN_CLIENT_SECRET: "rromVjiogKmHsT2ad3EjROgMi1aEceas"
  
  # NOTE: This is a PLACEHOLDER.
  # In production, use sealed-secrets, external-secrets, or vault.
  # For demo purposes, these default values match local development.
