apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  namespace: ecom
  labels:
    app: identity-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: identity-service
  template:
    metadata:
      labels:
        app: identity-service
        version: v1
    spec:
      containers:
      - name: identity-service
        image: ecommerce/identity-service:1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8083
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: common-config
        - configMapRef:
            name: identity-service-config
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: ecom-secrets
              key: DB_PASSWORD
        - name: KEYCLOAK_ADMIN_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: ecom-secrets
              key: KEYCLOAK_ADMIN_CLIENT_SECRET
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8083
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: ecom
  labels:
    app: identity-service
spec:
  type: ClusterIP
  selector:
    app: identity-service
  ports:
  - name: http
    port: 8083
    targetPort: 8083
    protocol: TCP
